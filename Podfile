# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'platonus' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for platonus

  pod 'PKHUD', '~> 5.0'
  pod 'SideMenuSwift'
  pod 'Alamofire', '~> 4.7'

  target 'platonusTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'platonusUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
